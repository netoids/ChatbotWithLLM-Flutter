specVersion: "v1beta"
serviceId: "dsi_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "dsi_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
